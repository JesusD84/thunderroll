services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend:/app:z
      - /app/node_modules
      - /app/.next
    command: ["npm", "run", "dev"]
